#ifndef SEARCH_MODE_H
#define SEARCH_MODE_H

#include <iostream>
#include <vector>

#include "cursor_control.h"
#include "util.h"

int search_mode(const std::string &query, std::string &extra_param);

#endif //SEARCH_MODE_H
